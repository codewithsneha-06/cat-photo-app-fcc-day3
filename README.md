# Cat Photo App - Day 3 of #100DaysOfCode

## What I Learned Today
- **HTML Structure**: `<main>`, `<section>`, `<h1>`, `<h2>`, `<p>` tags
- **Links**: `<a>` tag, `href` attribute, `target="_blank"` for new tabs
- **Images**: `<img>` tag, `src` and `alt` attributes  
- **Nesting**: Turned text into links + wrapped images in `<a>` tags
- **Debugging**: Fixed broken links and accidentally deleted elements

## Progress
✅ 20/42 steps completed on freeCodeCamp  
✅ First mini HTML project built from scratch

Built with 💚 as part of my #100DaysOfCode challenge.

## Live Code
Check `index.html` for the complete code.
